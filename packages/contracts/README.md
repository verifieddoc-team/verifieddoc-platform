# VerifiedDoc contracts

Shared TypeScript contracts for the web and mobile clients.

The API OpenAPI document remains the source of truth. This package contains the
client-facing shapes used by both applications so the two interfaces do not
drift while the backend contract is stable.
